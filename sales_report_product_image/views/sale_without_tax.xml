

<odoo>

<!-- <record id="action_report_saleorder_without_tax" model="ir.actions.report">
        <field name="name">Sale Order Without Tax</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">sales_report_product_image.report_sale_without_tax</field>
        <field name="report_file">sales_report_product_image.report_sale_without_tax</field>
        <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_sale_order"/>
        <field name="binding_type">report</field>
    </record> -->


<template id="report_sale_without_tax">
    <t t-call="web.html_container">
        <t t-foreach="docs" t-as="doc">
            <!-- Address Block -->
            <t t-set="address">
                <div t-field="doc.partner_id" t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}' />
                <p t-if="doc.partner_id.vat">
                    <t t-if="doc.company_id.account_fiscal_country_id.vat_label">
                        <t t-out="doc.company_id.account_fiscal_country_id.vat_label"/>
                    </t>
                    <t t-else="">Tax ID:</t> <span t-field="doc.partner_id.vat"/>
                </p>
            </t>

            <!-- Invoicing and Shipping Address Block -->
            <t t-if="doc.partner_shipping_id != doc.partner_invoice_id">
                <t t-set="information_block">
                    <strong>
                        <t t-if="doc.partner_shipping_id == doc.partner_invoice_id">
                            Invoicing and Shipping Address:
                        </t>
                        <t t-else="">Invoicing Address:</t>
                    </strong>
                    <div t-field="doc.partner_invoice_id" 
                        t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    <t t-if="doc.partner_shipping_id != doc.partner_invoice_id">
                        <strong>Shipping Address:</strong>
                        <div t-field="doc.partner_shipping_id" 
                            t-options='{"widget": "contact", "fields": ["address", "name", "phone"], "no_marker": True, "phone_icons": True}'/>
                    </t>
                </t>
            </t>

            <!-- Invoice Title and General Information -->
            <div class="page">
                <div class="oe_structure"/>
                <h2 class="mt-4">
                    <span t-if="env.context.get('proforma', False) or is_pro_forma">Pro-Forma Invoice # </span>
                    <span t-elif="doc.state in ['draft','sent']">Quotation # </span>
                    <span t-else="">Order # </span>
                    <span t-field="doc.name">SO0000</span>
                </h2>

                <div class="row mt-4 mb-2" id="informations">
                    <div t-if="doc.client_order_ref" class="col-auto col-3 mw-100 mb-2" name="informations_reference">
                        <strong>Your Reference:</strong><br/>
                        <span class="m-0" t-field="doc.client_order_ref">SO0000</span>
                    </div>
                    <div t-if="doc.date_order" class="col-auto col-3 mw-100 mb-2" name="informations_date">
                        <strong t-if="doc.state in ['draft', 'sent']">Quotation Date:</strong>
                        <strong t-else="">Order Date:</strong><br/>
                        <span class="m-0" t-field="doc.date_order" t-options='{"widget": "date"}'>2023-12-31</span>
                    </div>
                    <div t-if="doc.validity_date and doc.state in ['draft', 'sent']"
                         class="col-auto col-3 mw-100 mb-2" name="expiration_date">
                        <strong>Expiration:</strong><br/>
                        <span class="m-0" t-field="doc.validity_date">2023-12-31</span>
                    </div>
                    <div t-if="doc.user_id.name" class="col-auto col-3 mw-100 mb-2">
                        <strong>Salesperson:</strong><br/>
                        <span class="m-0" t-field="doc.user_id">Mitchell Admin</span>
                    </div>
                </div>

                <!-- Discount Logic -->
                <t t-set="lines_to_report" t-value="doc._get_order_lines_to_report()"/>
                <t t-set="display_discount" t-value="any(l.discount for l in lines_to_report)"/>

                <!-- Items Table -->
                <div class="oe_structure"></div>
                <table class="table table-sm o_main_table table-borderless mt-4">
                    <thead>
                        <tr>
                            <t t-if="doc.print_image and doc.image_sizes in ('image', 'image_medium', 'image_small')">
                                <th style="width:15%;">Product Image</th>
                            </t>
                            <th class="text-center" style="width:15%;">Item Code</th>
                            <th name="th_description" class="text-start">Description</th>
                            <th class="text-center" style="width:15%;">HSN Code</th>
                            <th name="th_quantity" class="text-end">Quantity</th>
                            <th class="text-center" style="width:15%;">Weight</th>
                            <th name="th_priceunit" class="text-end">Unit Price</th>
                            <th name="th_discount" t-if="display_discount" class="text-end"><span>Disc.%</span></th>
                            <th name="th_taxes" class="text-end">Taxes</th>
                            <th name="th_subtotal" class="text-end"><span>Amount</span></th>
                        </tr>
                    </thead>
                    <tbody >
                    <t t-set="current_subtotal" t-value="0"/>
                    <t t-foreach="doc.order_line" t-as="line">
                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>

                        <!-- Product Image -->
                        <t >
                            <t t-if="doc.print_image">
                                <td class="text-center">
                                    <t t-if="line.product_id.image_1920">
                                        <img t-att-src="'data:image/*;base64,' + line.product_id.image_1920"
                                            t-att-style="'width:' + (doc.image_sizes == 'image' and '95%' or doc.image_sizes == 'image_medium' and '70%' or doc.image_sizes == 'image_small' and '50%') + '; height:auto;'"/>
                                    </t>
                                </td>
                            </t>
                            <td class="text-center">
                                <t t-esc="line.product_id.barcode"/>
                            </td>
                            <td name="td_note_line" colspan="99">
                                <t t-esc="line.name"/>
                            </td>
                            <td class="text-center">
                                <t t-esc="line.product_id.l10n_in_hsn_code"/>                 
                            </td>
                            <t t-if="line.product_id.uom_id.category_id.name == 'Weight'">
                                <td class="text-center">
                                    <t t-esc="line.secondary_qty"/>
                                </td>
                            </t>
                        </t>
                    </t>
                </tbody>

                </table>

                <!-- Tax and Total Summary -->
                <!-- <div class="clearfix" name="so_total_summary">
                    <div id="total" class="row" name="total">
                        <div t-attf-class="#{'col-6' if report_type != 'html' else 'col-sm-7 col-md-6'} ms-auto">
                            <table class="table table-sm table-borderless">
                                <t t-set="tax_totals" t-value="doc.tax_totals"/>
                                <t t-call="sale.document_tax_totals"/>
                            </table>
                        </div>
                    </div>      
                </div> -->

                <!-- Signature Section -->
                <div t-if="doc.signature">
                    <div class="mt-4 ml64 mr4" name="signature">
                        <div class="offset-8">
                            <strong>Signature</strong>
                        </div>
                        <div class="offset-8">
                            <img t-att-src="image_data_uri(doc.signature)" style="max-height: 4cm; max-width: 8cm;"/>
                        </div>
                        <div class="offset-8 text-center">
                            <span t-field="doc.signed_by">Oscar Morgan</span>
                        </div>
                    </div>
                </div>

                <!-- Notes Section -->
                <div>
                    <span t-field="doc.note" t-attf-style="#{'text-align:justify;text-justify:inter-word;' if doc.company_id.terms_type != 'html' else ''}" name="order_note"/>
                    <p t-if="not is_html_empty(doc.payment_term_id.note)">
                        <span t-field="doc.payment_term_id.note">The payment should also be transmitted with love</span>
                    </p>
                    <div class="oe_structure"/>
                    <p t-if="doc.fiscal_position_id and not is_html_empty(doc.fiscal_position_id.sudo().note)" id="fiscal_position_remark">
                        <strong>Fiscal Position Remark:</strong>
                        <span t-field="doc.fiscal_position_id.sudo().note">No further requirements for this payment</span>
                    </p>
                </div>
            </div>
        </t>
    </t>
</template>



<!-- <record id="sale.action_sale_order_without_tax" model="ir.actions.report">
        <field name="name">Sale Order Without Tax</field>
    </record> -->

</odoo>
